@page
@model ZyPanel.Areas.Co.Pages.Product.IndexModel
@{
    ViewBag.Title = "قطعات";
    var createUrl = Url.Page("", "Create");
}

<partial name="_Alert" model="Model.Alert" />

<div class="table-container">
    <div class="table-container-header">
        <button class="btn btn-outline-dark" data-title="افزودن قطعه" data-url='@createUrl'
            onclick='ajaxLoadPartialForm(this)'>
            افزودن
        </button>
        <form method="GET">
            <input type="text" name="filter" class="form-control" placeholder="عنوان قطعه...">
            <input type="hidden" value="1" name="p" class="form-control">
            <button type="submit" class="btn btn-outline-dark">
                جستجو...
            </button>
        </form>
    </div>
    <div class="table-responsive" id="dataList">
        <table class="table table-bordered table-striped">
            <thead>
                <tr>
                    <th>#</th>
                    <th>قطعه</th>
                    <th>نوع ذوب</th>
                    <th>کد انبار</th>
                    <th>کد فنی</th>
                    <th>عملیات</th>
                </tr>
            </thead>
            <tbody>
                @if (Model.List.Any())
                {
                    foreach (var item in Model.List)
                    {
                        <tr>
                            <td>@item.Id</td>
                            <td>@item.Title</td>
                            <td>@item.Fusion</td>
                            <td data-null>@item.StoreCode</td>
                            <td data-null>@item.TechnicalCode</td>
                            <td data-change>
                                <div>
                                    <button type="button" data-edit data-title="ویرایش : @item.Title"
                                data-url='@Url.Page("","Edit",new {id=@item.Id})' onclick="ajaxLoadPartialForm(this)">
                                        <i class="bi"></i>
                                    </button>
                                </div>
                            </td>
                        </tr>
                    }
                }
                else
                {
                    <tr data-span></tr>
                }
            </tbody>
        </table>
        <partial name="_Pagination" model='(Model.List.TotalPages)' />
    </div>
</div>